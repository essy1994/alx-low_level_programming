#include "main.h"
#include <string.h>
/**
 *  * _strlen_recursion - length of string
 *   * @s: string to count
 *    * Return: length of string
**/
int _strlen_recursion(char *s)
{
   return (strlen(s)); 
}
